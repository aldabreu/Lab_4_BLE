#include <msp430.h>
#include "Mem_Manager.h"

/*
 * main.c
 */
int main(void) {
    WDTCTL = WDTPW | WDTHOLD;	// Stop watchdog timer

   // osal_mem_init();
    void * voidarr[2];
    int i = 0;
    for(;i < 2;i++)
    	voidarr[i] = NULL;
		while(1);

	return 0;
}
