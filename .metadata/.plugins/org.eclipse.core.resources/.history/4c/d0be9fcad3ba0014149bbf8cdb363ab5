#include <msp430.h>
#include "Mem_Manager.h"

/*
 * main.c
 */
int main(void) {
    WDTCTL = WDTPW | WDTHOLD;	// Stop watchdog timer

    osal_mem_init();
    //osal_mem_kick();
    osal_mem_alloc(200);


while(1);


	return 0;
}
