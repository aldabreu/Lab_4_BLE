#include <msp430.h>
#include "Mem_Manager.h"

/*
 * main.c
 */
int main(void) {
    WDTCTL = WDTPW | WDTHOLD;	// Stop watchdog timer

   // osal_mem_init();

int arr[1] = {0};
while(1);

	return 0;
}
