#include <msp430.h>
#include "Mem_Manager.h"

/*
 * main.c
 */
int main(void) {
    WDTCTL = WDTPW | WDTHOLD;	// Stop watchdog timer

   // osal_mem_init();
printf("Hello world");


while(1);

	return 0;
}
