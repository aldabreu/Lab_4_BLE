#include <msp430.h>
#include "Mem_Manager.h"
#include <assert.h>
/*
 * main.c
 */
int main(void) {
    WDTCTL = WDTPW | WDTHOLD;	// Stop watchdog timer

   // osal_mem_init();


assert(1==2);
while(1);

	return 0;
}
